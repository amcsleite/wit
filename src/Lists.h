#pragma once

class Term;
class Expression;
class ExpressionList;

typedef SmartPtr<ExpressionList> ExpressionListPtr;
typedef SmartPtr<Expression> ExpressionPtr;
typedef SmartPtr<Term> TermPtr;

// eof
